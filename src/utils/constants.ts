export const STORAGE_KEYS = {
  USER_PROGRESS: 'vision_tree_progress_v2',
} as const;

export const XP_PER_NEW_NODE = 25;
export const XP_DEPTH_BONUS = 10; // extra per depth level
export const XP_BRANCH_COMPLETE_BONUS = 100;
